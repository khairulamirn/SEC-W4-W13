function add (a, b) {
    return a + b;
};

function substract (a, b) {
    return a - b;
};

const math = {
    add,
    substract
};

// module.exports = math;

// using export default
export default math;