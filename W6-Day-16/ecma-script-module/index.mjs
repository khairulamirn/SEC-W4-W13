// const math = require('./math.js');
import math from './math.mjs';

const addAnswer = (math.add(16, 26));
const substractAnswer = (math.substract(16, 26));

console.log(addAnswer);
console.log(substractAnswer);